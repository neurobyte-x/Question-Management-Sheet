
This project is a basic React app created using Vite.

Vite is used because it is fast and provides hot module replacement (HMR), which means the app updates instantly while developing without reloading the page.

The project uses the standard React plugin for Vite and includes ESLint to help maintain basic code quality.

This setup is meant only for assessment and practice purposes, not for production use.